# 性能验证记录（2026-07-08）

目标：验证“MCU 本地 1kHz 起步、阶梯到 10kHz 控制基线 + PC/ROS 高频下发 + ROS 状态/链路健康回传”的第一阶段可行性。

## 环境

- 板子：STM32F103RB，USART1 `/dev/ttyUSB0`，921600 8N1，DMA。
- Agent：`tools/run-bridge.sh /dev/ttyUSB0 921600`，默认 `MICROROS_AGENT_VERBOSITY=1`。
- 固件：新增 `com_control_task` 1kHz 本地控制骨架；2/5/10kHz 档切换到 TIM2 ISR；micro-ROS spin/read 等待收紧到 1ms 量级。
- PC 节点：`pc_cmd.launch.py`，QoS `RELIABLE / KEEP_LAST / depth=2`，早期压测使用 `rtt_warn_ms=10`，`rtt_deadline_ms=50`；当前默认诊断 hard deadline 为 `120ms`，并启用 `startup_grace_s=3.0` 排除 PC 节点刚启动时的发现/握手瞬态。高频 latest-target profile 仍显式使用 `best_effort / depth=1`。

## 结果

### Reliable profile

| PC 下发频率 | `/com/tp_mcu_status` 观测频率 | LinkHealth 结果 |
|---:|---:|---|
| 20 Hz | 20.0 Hz | 稳定，`sent=220 matched=219 lost=0 inflight=1` |
| 30 Hz | 29.7 Hz | 吞吐基本跟上，但出现丢包，末尾 `lost=27` |
| 40 Hz | 37.2 Hz | 吞吐不足，出现丢包/重复，末尾 `lost=21 duplicate=2` |
| 50 Hz | 42.2 Hz | 明显进入瓶颈，末尾 `lost=99` |
| 75 Hz | 42.1 Hz | 大量丢包，末尾 `lost=360` |
| 100 Hz | 39.1-45.5 Hz | 大量丢包，末尾 `lost=509` |
| 200 Hz | 39.2 Hz | 严重拥塞，`sent=2144 matched=383 lost=1756 duplicate=93` |

### Best-effort profile

固件以 `-DEXO_QOS_BEST_EFFORT=ON` 编译，PC 以
`qos_reliability:=best_effort qos_depth:=1` 运行。

| PC 下发频率 | `/com/tp_mcu_status` 观测频率 | LinkHealth 结果 |
|---:|---:|---|
| 50 Hz | 40.9 Hz | 仍有丢包，末尾 `sent=523 matched=436 lost=86` |
| 100 Hz | 86.4 Hz | topic 吞吐提高，但配对丢失严重，末尾 `sent=963 matched=278 lost=681` |
| 200 Hz | 79.9 Hz | topic 吞吐约 80Hz，配对丢失严重，末尾 `sent=1895 matched=203 lost=1682` |

### 2Mbps reliable profile

固件以 `-DEXO_UART_BAUD=2000000` 编译，Agent 以 `tools/run-bridge.sh /dev/ttyUSB0 2000000` 运行。

| PC 下发频率 | `/com/tp_mcu_status` 观测频率 | LinkHealth 结果 |
|---:|---:|---|
| 20 Hz | 20.0 Hz | 基本稳定，但出现过 1 次 lost，末尾 `sent=227 matched=225 lost=1` |
| 50 Hz | 46.3 Hz | 比 921600 略高，但仍丢包，末尾 `sent=517 matched=451 lost=65` |
| 100 Hz | 45.7 Hz | 大量丢包，末尾 `sent=917 matched=428 lost=485` |

### MCU local control sweep

固件以 `-DEXO_CONTROL_LOOP_HZ=<hz>` 编译，Agent 保持
`tools/run-bridge.sh /dev/ttyUSB0 921600`，PC 侧以 20Hz reliable 烟测。
本地 tick 通过 `tools/measure-control-loop.sh 5` 从 SWD 读取
`g_control_tick_count` 两次估算。

| 本地控制目标 | SWD tick 估算 | ROS 图 | 20Hz LinkHealth 结果 | RTT warn 统计 |
|---:|---:|---|---|---|
| 2 kHz | 2081.40 Hz | `/node_com_mcu`，`/com/tp_cmd_heartbeat`，`/com/tp_mcu_status` | `sent=143 matched=142 lost=0 duplicate=0 inflight=1` | warn 160 次，avg 26.207ms，max 71.789ms |
| 5 kHz | 5206.60 Hz | 同上；`ros2 node list` 偶发显示重复 `/node_com_mcu` | `sent=163 matched=162 lost=0 duplicate=0 inflight=1` | warn 169 次，avg 25.996ms，max 68.498ms |
| 10 kHz | 10366.40 Hz | 同上；`ros2 node list` 偶发显示重复 `/node_com_mcu` | `sent=160 matched=159 lost=0 duplicate=0 inflight=1` | warn 160 次，avg 25.559ms，max 82.670ms |

注意：SWD/GDB attach 会短暂停核，所以该工具适合做档位量级确认，不适合做精密抖动测量。精密测量仍需要 GPIO 翻转 + 逻辑分析仪/示波器。

### Latest-target status decimation

新增固件编译项 `EXO_STATUS_EVERY_N`：MCU 每条 PC 命令都更新本地 latest target，
但只按比例发布 `/com/tp_mcu_status`。这更贴近“PC 200Hz 下发目标，MCU 本地闭环消费最新目标，状态/链路健康另行回传”的控制分层。

| QoS / 配置 | PC 下发 | 状态回传频率 | 估算 MCU 收到 PC 目标 | 结果 |
|---|---:|---:|---:|---|
| reliable，`EXO_STATUS_EVERY_N=5` | 200 Hz | 约 14.4 Hz | 约 72 Hz | 比 full echo 约 40Hz 有改善，但仍远低于 200Hz |
| best-effort，`EXO_STATUS_EVERY_N=5` | 200 Hz | 约 32.3 Hz | 约 161 Hz | 当前最接近 200Hz 的 topic 模式 |

PC 侧新增 `tracking_mode:=sampled` 后，LinkHealth 不再预设“每条命令都必须 echo”，而是记录最近发出的 seq；实际状态回来后按 seq 回溯发送时间并统计 RTT。

| QoS / 波特率 / 配置 | PC 下发 | 状态回传频率 | 估算 MCU 收到 PC 目标 | LinkHealth |
|---|---:|---:|---:|---|
| best-effort，921600，`status_every_5`，sampled | 200 Hz | 约 30.7 Hz | 约 153 Hz | `lost=0 duplicate=0 inflight=0` |
| best-effort，2Mbps，`status_every_5`，sampled | 200 Hz | 约 31.9 Hz | 约 160 Hz | `lost=0 duplicate=0 inflight=0` |
| best-effort，3Mbps，`status_every_5`，sampled | 200 Hz | 约 28.6 Hz | 约 143 Hz | `lost=0 duplicate=0 inflight=0`，出现 0.845s 长间隔 |
| best-effort，921600，`status_every_10`，sampled | 200 Hz | 约 17.6 Hz | 约 176 Hz | `lost=0 duplicate=0 inflight=0` |
| best-effort，921600，`status_every_20`，sampled | 200 Hz | 约 9.26 Hz | 约 185 Hz | `lost=0 duplicate=0 inflight=0` |
| best-effort，921600，`status_every_40`，sampled | 200 Hz | 约 4.81 Hz | 约 192 Hz | `lost=0 duplicate=0 inflight=0`；曾遇到一次 ST-Link flash loader 瞬态失败，复位停核后重烧成功 |
| best-effort，921600，`status_every_40`，sampled，脚本回归 | 200 Hz | 4.755 Hz | 190.20 Hz | `sent=95 matched=95 lost=0 duplicate=0 inflight=0` |

同一 latest-target profile 叠加 MCU 本地闭环阶梯：

| 本地控制目标 | PC 下发 | 状态回传频率 | 估算 MCU 收到 PC 目标 | LinkHealth | 备注 |
|---:|---:|---:|---:|---|---|
| 2 kHz | 200 Hz | 4.840 Hz | 193.60 Hz | `sent=95 matched=95 lost=0 duplicate=0 inflight=0` | RAM 13992B，TIM2 ISR 无 control task stack |
| 5 kHz | 200 Hz | 4.477 Hz | 179.08 Hz | `sent=92 matched=92 lost=0 duplicate=0 inflight=0` | 出现一次 0.880s 状态长间隔；前半段约 4.8Hz |
| 10 kHz | 200 Hz | 4.827 Hz | 193.08 Hz | `sent=95 matched=95 lost=0 duplicate=0 inflight=0` | RAM 13992B，通信统计 clean |

因此 sampled/latest-target 模式的监控语义已经对齐：未抽样回传的目标不再被误报 lost。把状态回传降到 5Hz 量级后，MCU 接收 PC 目标已经接近 200Hz；2Mbps/3Mbps 没有带来线性收益，当前收益最大的方向是“命令 latest-only 高频下发，状态/诊断低频回传”。

继续围绕 10kHz 本地闭环、PC 200Hz 下发做状态降频与 PC 侧 timer 负载验证：

| 配置 | 状态回传频率 | 估算 MCU 收到 PC 目标 | 最大状态间隔 | PC wire rate | LinkHealth | 结论 |
|---|---:|---:|---:|---:|---|---|
| `status_every_50` | 3.702 Hz | 185.10 Hz | 1.069s | 未记录 | `sent=122 matched=122 lost=0 duplicate=0 inflight=0` | 继续降状态频率反而引入长间隔，不作为当前推荐 |
| `status_every_40`，20s 复测 | 4.604 Hz | 184.16 Hz | 1.145s | 未记录 | `sent=153 matched=153 lost=0 duplicate=0 inflight=0` | 可复现偶发长间隔，需区分真链路与 CLI/调度观测 |
| `status_every_40`，新增 PC wire 统计 | 4.894 Hz | 195.76 Hz | 0.297s | 191.013 Hz | `sent=152 matched=152 lost=0 duplicate=0 inflight=0` | PC 实际发送低于 200Hz，链路状态 clean |
| `status_every_40`，`executor_threads=2` | 4.657 Hz | 186.28 Hz | 0.897s | 192.056 Hz / window 192.991 Hz | `sent=154 matched=154 lost=0 duplicate=0 inflight=0` | 固定 2 线程未改善 |
| `status_every_40`，`sweep_period_s=0.02` | 4.885 Hz | 195.40 Hz | 0.233s | 193.595 Hz / window 191.500 Hz | `sent=150 matched=150 lost=0 duplicate=0 inflight=0` | 当前最干净；默认 deadline sweep 从 5ms 放宽到 20ms，降低 PC 侧 timer 负载 |
| `status_every_40`，日志降噪后复测 | 4.846 Hz | 193.84 Hz | 0.249s | 194.348 Hz / window 199.026 Hz | `sent=139 matched=139 lost=0 duplicate=0 inflight=0` | 高频稳态窗口已接近 200Hz；全程平均受启动/预热影响 |
| `status_every_40`，60s soak，`summary_period_s=5` | sampler 4.827 Hz | sampler 193.08 Hz / PC 累计 193.382 Hz | sampler 0.264s | wire 194.088 Hz / window 196.007 Hz | `sent=349 matched=349 lost=0 duplicate=0 inflight=0` | 长时复测 clean；CLI `ros2 topic hz` 仍报 1.362s 长间隔，但 sampler 不复现 |
| `status_every_40`，`cmd_catchup_max=1`，60s soak | sampler 4.980 Hz | sampler 199.20 Hz / PC 累计 199.243 Hz | sampler 0.333s | wire 200.003 Hz / window 200.066 Hz | `sent=341 matched=341 lost=0 duplicate=0 inflight=0` | PC 侧补偿 rclpy timer 长期偏慢，当前最接近 200Hz 的推荐 profile |
| `status_every_40`，`cmd_catchup_max=1`，seq sampler | sampler 4.979 Hz | sampler 199.16 Hz / seq 199.994 Hz / PC 累计 199.092 Hz | sampler 0.242s | wire 199.997 Hz / window 199.998 Hz | `sent=341 matched=341 lost=0 duplicate=0 inflight=0` | `seq_delta_avg=40.165/min=40/max=47`，可见 best-effort 下存在轻微跳号但总体 200Hz 节奏成立 |
| `status_every_40`，PC 复用 ExoCmd 消息对象，复测 | sampler 4.984 Hz | sampler 199.36 Hz / seq 200.006 Hz / PC 累计 199.131 Hz | sampler 0.216s | wire 199.997 Hz / window 200.013 Hz | `sent=345 matched=345 lost=0 duplicate=0 inflight=0` | `seq_delta_avg=40.131/min=40/max=43`；复用对象降低 PC 侧短命分配，不改变线速瓶颈 |

## 结论

- 当前 `RELIABLE + micro-ROS serial + 921600 + F103` 往返闭环稳定点约为 20 Hz。
- 200 Hz 目标未达成；瓶颈不是单一 PC timer 或 agent 日志，固件已收紧到 1ms 后仍卡在约 40 Hz 状态回传。
- best-effort 能提高裸 topic 吞吐到约 80 Hz，但不能直接满足“每条控制命令可配对确认”的安全闭环要求。
- 2Mbps 能建链，说明物理层可跑更高波特率；但 reliable 闭环只从约 42Hz 提到约 46Hz，收益不足以支撑 200Hz。
- `EXO_CONTROL_LOOP_HZ=1000/2000/5000/10000` 四档已通过编译验证；2/5/10kHz 使用 TIM2 ISR，不占 control task 栈，RAM 从 15.6KB 降到约 15.0KB。
- 2/5/10kHz 真机运行期烟测均能保持 20Hz PC 命令无 lost；RTT 仍主要落在 20-30ms 区间，说明“MCU 本地闭环提频”和“PC/ROS 往返通信延迟”需要分开优化。
- latest-target 方向有实质收益：`best_effort + status_every_40 + sampled monitor` 反推 MCU 可接收约 190Hz PC 目标；叠加 2kHz/10kHz 本地闭环后仍可保持约 193Hz 等效目标接收。进一步开启 PC 侧 `cmd_catchup_max=1` 后，10kHz/200Hz/status_every_40 60s soak 达到 sampler 等效 `199.20Hz`、PC 累计 `199.243Hz`、`lost=0 duplicate=0`，已经基本达到“PC/ROS 200Hz 下发目标，MCU 本地闭环消费 latest target”的目标形态。继续降到 `status_every_50` 没有收益，当前推荐维持 `status_every_40`。
- 新增 `tools/measure-stack-hwm.sh` 后读到默认 1024-word micro-ROS 栈实际 used≈484/free≈540 words；据此把 `MICROROS_TASK_STACK_WORDS` 从 1024 降到 768，RAM 从 15616B 降到 14592B，仍有 free≈284 words。注意该脚本会通过 GDB 短暂停核，不应和实时 hz 测量同时使用。
- 新增 `tools/run-com-perf.sh` 后，真机 profile 可一键复现：脚本会构建/烧录、启动 bridge、启动 PC node、采样 `/com/tp_mcu_status` 并输出 LinkHealth summary。
- 恢复默认固件后，20Hz smoke 可建图并正常收发；连续短测出现过单次 `lost=1 duplicate=1`，RTT 约 55ms，需继续观察是否为 reset 后瞬态或链路抖动。后续单独复测 20Hz 为 `sent=140 matched=139 lost=0 duplicate=0 inflight=1`。脚本化默认回归状态频率仍为 19.986Hz，但出现 `sent=240 matched=233 lost=6 duplicate=5 inflight=1`，说明 reliable/full-echo 模式的重传/超时抖动仍需继续定位；不影响 latest-target/best-effort 分层方向的结论。
- 上述 default reliable 的 `lost/duplicate` 中，日志显示典型 late echo 在 50-85ms 区间，50ms deadline 太贴近 20Hz 周期与 RTT 尾部；因此后续默认监控先改为 `rtt_warn_ms=10`、`rtt_deadline_ms=100`：10ms 仍作为滞后风险预警，100ms 才判 LOST。改为 100ms 后脚本回归状态频率 20.010Hz，末尾 `sent=248 matched=246 lost=1 duplicate=0 inflight=1`；剩余 1 次为 `seq=33 waited_ms=103.945`，属于真实长尾超时，后续仍需继续定位 full-echo reliable 的偶发 >100ms 抖动。
- PC 节点新增 `wire_sent/wire_rate_hz/wire_window_hz` 日志后确认：200Hz 目标下，rclpy 实际 wire 发送约 191-194Hz；把 deadline sweep 从 5ms 放宽到 20ms 后，状态间隔更稳定（本轮 max 0.233s），因此 `pc_cmd.launch.py` 与 `tools/run-com-perf.sh` 默认 `sweep_period_s=0.02`。
- PC 节点默认关闭逐条 matched INFO 日志，并把 soft RTT WARN 日志按 1s 节流后，default reliable smoke 改善为 `status_hz=19.897`、`max_gap_s=0.076`、`wire_window_hz=20.000`、`sent=243 matched=242 lost=0 duplicate=0 inflight=1`。后续恢复默认固件后一轮短测出现过 `max_gap_s=1.197/lost=1`，立即不烧录复测恢复到 `status_hz=20.004`、`max_gap_s=0.062`、`lost=0 duplicate=0 inflight=1`；说明偶发长尾仍存在，但热路径日志降噪有实际收益，后续高频 profile 默认保持降噪。
- 为避免 `ros2 topic hz` 单一 CLI 视角误判，PC summary 新增 `sent_window_hz/matched_window_hz/target_window_hz`，`tools/run-com-perf.sh` 会解析最后一个 summary 窗口。默认 reliable 复测得到 `status_hz=19.997`、`pc_matched_window_hz=19.999`、`pc_target_window_hz=19.999`、`max_gap_s=0.073`、`lost=0 duplicate=0 inflight=1`，两种观测口径一致。
- 用窗口指标复测高频推荐 profile（10kHz 本地闭环、200Hz PC、best-effort、`status_every_40`）：`status_hz=4.841`、`pc_matched_window_hz=4.999`、`pc_target_window_hz=199.972`、`max_gap_s=0.248`、`lost=0 duplicate=0 inflight=0`。这说明稳态窗口已达到“PC/ROS 200Hz 下发目标，MCU 本地闭环消费 latest target”的目标形态。
- 恢复默认 reliable 后连续两轮短测仍可复现 `ros2 topic hz` 约 1.15s 长间隔与累计 `lost=1`，但最后一个 PC summary 窗口保持 `pc_target_window_hz≈20`；这类偶发长尾/CLI 观测差异仍列为未解决项，需要更长 soak 或非 CLI 采样器确认根因。
- 新增 `ros2 run exo_cmd status_sampler` 作为非 CLI 采样器，并接入 `tools/run-com-perf.sh`。默认 reliable smoke 中 sampler 与 `ros2 topic hz` 一致：`sampler_hz=19.997`、`sampler_max_gap_s=0.063`、`status_hz=19.995`、`lost=0 duplicate=0 inflight=1`。后续判断长尾时以 sampler + PC summary + CLI 三个视角交叉验证。
- 用 sampler 复测高频推荐 profile 时，CLI 再次出现 `max_gap_s=1.268` 并把等效目标接收拉低到 `183.12Hz`，但 sampler 记录 `sampler_hz=4.845`、`sampler_max_gap_s=0.232`、等效 `sampler_target_rx_hz=193.80`，PC summary 末窗口 `pc_target_window_hz=199.972`、`lost=0 duplicate=0 inflight=0`。因此当前判断：CLI 长间隔主要是观测工具抖动；高频链路稳态窗口仍接近 200Hz。
- `tools/run-com-perf.sh` 现在解析 PC summary 的累计 `target_rate_hz`，避免 `status_every_40` 在 1s summary 窗口里因 4/5 个样本边界抖动而误读成 160/200Hz 跳变。复测 10kHz/200Hz/best-effort/status_every_40，60s sampler 为 `4.827Hz`（等效 `193.08Hz`），PC 累计 `pc_target_rate_hz=193.382`，LinkHealth `sent=349 matched=349 lost=0 duplicate=0 inflight=0`；CLI 仍显示 `max_gap_s=1.362`，因此高频推荐 profile 的判据以后以 sampler + PC 累计 rate 为主。
- PC 节点新增 `cmd_catchup_max`，默认 0；高频 latest-target profile 显式使用 `cmd_catchup_max=1`，在 rclpy timer 晚到时最多补发 1 条命令，避免长期 wire rate 停在约 194Hz。复测结果：`pc_wire_rate_hz=200.003`、`pc_target_rate_hz=199.243`、`sampler_target_rx_hz=199.20`、`lost=0 duplicate=0 inflight=0`。该补偿不作为 reliable/full-echo 默认值，避免用突发掩盖诊断链路调度问题。
- `status_sampler` 新增 status header.seq 步长统计，并修正 `tools/run-com-perf.sh` 的 key 解析，避免 `seq_rate_hz` 被误读成 `rate_hz`。复测 10kHz/200Hz/best-effort/status_every_40/catchup=1：`sampler_hz=4.979`、按收到 status 反推 MCU 目标接收约 `199.16Hz`，status seq 进度 `seq_rate_hz=199.994`，`seq_delta_avg=40.165/min=40/max=47`，PC `wire_rate_hz=199.997`，LinkHealth `sent=341 matched=341 lost=0 duplicate=0 inflight=0`。结论：PC wire 节奏已贴近 200Hz；MCU 侧 latest-target 有约 0.4% 的跳号空间，符合 best-effort 性能 profile，但若未来控制命令不允许跳号，需要单独设计可靠控制通道或更小 payload/更高带宽。
- PC 节点发布热路径改为复用一个 `ExoCmd` 消息对象，避免 200Hz 下每条命令都新建 Python 消息对象。节点测试覆盖“同一对象 id 重用，但 publish 时刻的 seq/payload 仍连续”。真机 10kHz/200Hz/status_every_40/catchup=1 复测第一轮出现一次主机侧长尾（`sampler_max_gap_s=0.795`、`seq_delta_max=159`），同固件不重刷第二轮恢复为 `sampler_hz=4.984`、`seq_rate_hz=200.006`、`seq_delta_avg=40.131/min=40/max=43`、`lost=0 duplicate=0`。结论：对象复用可以降低 PC 分配/GC 压力，但不单独解决偶发调度长尾，后续仍需多轮 soak 或进程调度策略验证。
- 默认 reliable 固件恢复后用 `tools/measure-stack-hwm.sh` 读运行时栈水位：`microros_task_stack used=484/free=284 words`（总 768）、`control_task_stack used=27/free=101`（总 128）、`led_task_stack used=22/free=42`（总 64）、`idle_task_stack used=24/free=104`（总 128）。这说明继续压缩 SRAM 有空间，但 micro-ROS 栈若从 768 降到 640 只剩约 156 words 裕度，需先在 200Hz 高频 profile 后复测水位。尝试做高频栈 probe 时 ST-LINK 出现连续 `LIBUSB_ERROR_TIMEOUT`，MCU/agent 仍在线，判定为 SWD/USB 调试通道瞬态问题；已给 `tools/run-com-perf.sh` 增加 `FLASH_TIMEOUT_SECONDS` 和 `STLINK_PREFLIGHT`。当前故障现场下 preflight 在约 12s 内识别 `chipid=0x000/dev-type=unknown` 并快速退出，避免无人值守时先 build 再卡 flash。`tools/measure-stack-hwm.sh` 也加入同类 ST-LINK 预检，并给每次 GDB 读内存设置超时；当前坏 SWD 现场下同样约 12s 快速退出。高频栈水位复测保留为未解决项。
- 恢复默认 reliable 后 smoke：`status_hz=19.993`、`sampler_hz=20.002`、`sampler_max_gap_s=0.065`、`pc_target_window_hz=20.003`，但 LinkHealth 累计 `lost=1`；reliable full-echo 偶发 lost 仍保留为未解决项，不影响 latest-target 分层方向。
- default reliable 60s soak 对比：`rtt_deadline_ms=100` 时 sampler 稳定在 `20.029Hz`、`sampler_max_gap_s=0.076`，但 full-echo 诊断有 2 次刚过线的 LOST（`waited_ms=103.311/100.382`）；同条件把 hard deadline 提到 `120ms` 后，sampler 为 `20.023Hz`、`sampler_max_gap_s=0.084`，PC 窗口 `pc_target_window_hz=19.997`，LinkHealth 为 `sent=1454 matched=1453 lost=0 duplicate=0 inflight=1`。因此当前默认 hard deadline 调整为 `120ms`，但这只代表 full-echo 诊断的 LOST 判定阈值；控制滞后风险仍看 `rtt_warn_ms=10`、sampler gap 和 PC summary。
- 新增 `startup_grace_s` 后，PC 节点启动时仍照常发布命令以预热 DDS/XRCE/MCU 通路，但这些 seq 不进入 LinkHealth 统计，grace 回显晚到也不会污染 duplicate/unmatched。0.5s 可消除 `seq=0` 误报，但一次 cold-ish quick smoke 显示前约 2s 仍可能尚未稳定并累计 41 个启动期 LOST，因此默认 grace 调整为 3.0s。默认诊断统计的目标是稳态链路质量，不把 PC 节点刚启动的发现/匹配阶段计入累计 lost。
- reliable/full-echo QoS depth 对比：默认 depth 1 在 60s soak 中仍出现一次运行期 `LOST seq=739 waited_ms=136.210`，且无 duplicate，说明更像单个 echo 没进入 PC LinkHealth 回调，而不是单纯晚到；同固件不重烧复测 depth 4 为 `sampler_hz=20.016`、`pc_target_rate_hz=20.013`、`lost=0 duplicate=0`，depth 2 为 `sampler_hz=20.031`、`pc_target_rate_hz=20.025`、`lost=0 duplicate=0`。因此 reliable/full-echo 默认 QoS depth 调整为 2；高频 latest-target/best-effort 控制流仍显式保持 depth 1，避免 stale target 排队。
- 新默认 `qos_depth=2/startup_grace_s=3.0` quick smoke：`status_hz=20.000`、`sampler_hz=20.000`、`sampler_max_gap_s=0.066`、PC 累计 `pc_target_rate_hz=20.000`，LinkHealth `sent=547 matched=546 lost=0 duplicate=0 inflight=1`。这作为当前默认 reliable baseline。
- `status_sampler` 的 `spin_once` 等待上限从固定 50ms 改为可配置 `--spin-timeout-s`，`tools/run-com-perf.sh` 默认传 `0.005s` 并在日志打印。该改动不改变收到消息的时间戳来源，但会把采样结束量化误差从最多约 50ms 降到约 5ms，后续 200Hz/低频 status profile 的 rate 判读更稳。
- `tools/run-com-perf.sh` 新增 `RESET_TARGET`，默认跟随 `FLASH_FIRMWARE`；因此 `BUILD_FIRMWARE=0 FLASH_FIRMWARE=0` 的串口/ROS 复测路径不会再调用 OpenOCD reset。当前 ST-LINK/SWD 异常但 MCU 串口链路在线时，可继续做 no-flash 通信复测，不被调试通道卡住。现场 no-flash smoke 已跑通：`/node_com_cmd`、`/node_com_mcu` 均在线，`status_hz=20.005`、`sampler_hz=20.001`、`seq_delta_avg/min/max=1/1/1`、LinkHealth `sent=196 matched=195 lost=0 duplicate=0 inflight=1`。
- 新增 `tools/firmware-size-report.sh` 汇总固件 `size`、静态任务栈和最大 RAM/Flash 符号。当前默认 ELF 基线：`flash_bytes=82376`、`ram_static_bytes=14608`，最大 RAM 符号为 `microros_task_stack=3072B`、`custom_sessions=1192B`、若干 generated type-description 数据。后续 SRAM 优化先用该脚本量化，再结合运行期栈水位决定是否压栈。
- 新增 `tools/run-com-staircase.sh` 固化分阶梯验证顺序：`baseline_1khz_20hz_reliable`，再依次跑 `latest_1000/2000/5000/10000hz_200hz_best_effort_status_every_40`。`DRY_RUN=1` 已验证完整矩阵会按预期展开；当前坏 SWD 现场下，脚本会记录 `chipid=0x000/dev-type=unknown` 并跳过需要烧录的阶梯，fallback 到 no-flash smoke。短测结果：`status_hz=20.000`、`sampler_hz=20.024`、`seq_delta_avg/min/max=1/1/1`、LinkHealth `sent=190 matched=188 lost=0 duplicate=0 inflight=2`。
- `status_sampler` 新增 receive-gap `p95_gap_s/p99_gap_s`，`tools/run-com-perf.sh` 会解析并打印，便于区分普通抖动和长尾。no-flash 短测结果：`sampler_hz=20.030`、`sampler_p95_gap_s=0.052`、`sampler_p99_gap_s=0.062`、`sampler_max_gap_s=0.062`、LinkHealth `lost=0 duplicate=0 inflight=1`。
- `tools/run-com-staircase.sh` 现在会把每个完成阶段的关键 `[com-perf]` 指标提取成一行 `METRICS` 写入 summary，包含 `status_hz/sampler_hz/sampler_target_rx_hz/p95_gap/p99_gap/max_gap/seq_delta/pc_target_rate_hz/lost/duplicate/inflight`。当前坏 SWD fallback 短测 summary 已生成：`sampler_hz=20.001`、`sampler_p95_gap_s=0.051`、`sampler_p99_gap_s=0.056`、`lost=0 duplicate=0 inflight=1`。
- 新增 `tools/summarize-com-staircase.sh`，可把 staircase summary 的 `METRICS` 行转成 Markdown 或 CSV，便于完整 1k/2k/5k/10k 矩阵跑完后直接比较每档收益。已用 fallback summary 验证输出：`no_flash_smoke, status_hz=19.997, sampler_hz=20.001, p95_gap_s=0.051, p99_gap_s=0.056, lost=0, duplicate=0`。
- `status_sampler` 新增 `zero_gap_count`，用于标记接收间隔 <=0.5ms 的贴边双回调/采样异常。no-flash 短测中 sampler 为 `sampler_hz=19.997`、`p95_gap_s=0.052`、`p99_gap_s=0.062`、`zero_gap_count=0`，而同轮 `ros2 topic hz` CLI 报 `min=0.000/max=0.373`，进一步说明高频判据应以 sampler + PC summary 为主，CLI 仅作辅助视角。
- 后续优化应优先验证：
  - 进一步拆分控制命令与诊断状态：控制命令保持 latest-only / best-effort，状态诊断考虑降频或独立通道。
  - XRCE stream history、MTU、baudrate 对吞吐的影响。
  - MCU 本地控制闭环继续保持独立，PC/ROS 不参与每个 motor-control tick。

# 项目状态与交接(给跨会话/跨发行版接手的 Claude 读)

> 这份文档用于在「换 WSL 发行版 / 开新会话」后快速恢复上下文。读完它你就知道项目是什么、定了哪些决策、现在卡在哪、下一步做什么。

## 项目是什么
- 个人项目:**外骨骼机器人**。用户(lhqonly)边做边学怎么用好 Claude Code。
- 用 Claude Code 子 agent 模拟小型研发团队(详见 `.claude/agents/README.md`):
  - **elon** = 项目经理/架构师(规划、拆任务卡,不写功能代码)
  - **tom** = 开发(按任务卡写代码)
  - **gill** = 测试(独立交叉验证;可调用 `tools/codex-review.sh` 用 Codex/GPT 做对抗性第二意见)

## 第一个需求(进行中)
在 WSL(Ubuntu 24.04)跑 **ROS2**,在 STM32 跑 **micro-ROS**,二者通过**串口**连接,实现**双向 pub/sub**(先打通最小闭环)。

## 当前快照（2026-07-09，性能验证/优化进行中）
- 命名已切到通信域：ROS 节点 `/node_com_cmd`、`/node_com_mcu`；topic `/com/tp_cmd_heartbeat`、`/com/tp_mcu_status`、`/com/tp_link_health`。启动包为 `com_bringup`，PC 桥脚本为 `tools/run-bridge.sh`。
- 目标架构已收敛为分层控制：MCU 本地闭环 1kHz 起步、可编译验证到 2/5/10kHz；PC/ROS 以 200Hz 下发 latest target；MCU 用最新目标做本地插值/限幅/安全保护；ROS 侧接收状态和链路健康。
- M2 `/motor` micro-ROS 实体已进入实验 profile：`46009c7` 接入 `/motor/tp_joint_target`、`/motor/tp_joint_state`、`/motor/tp_motor_health` 并重建 motor 版 `libmicroros.a`；`975b2d7` 加固非空 `header.frame_id` 接收缓冲和 TIM2 高优先级下的 telemetry snapshot；`7bba9ca` 把 motor 通信预算纳入推荐脚本。默认固件仍 `EXO_MOTOR_ROS_ENTITIES=OFF`，`/com` 高速验证不能直接等同于 `/motor` topic 验收。
- M2 当前剩余验收：先用 `tools/recommend-motor-m2-smoke-command.sh` 生成真机 smoke 命令并采集 `log/motor-m2-smoke/<tag>/` evidence，再用 `tools/check-motor-m2-smoke-evidence.py <evidence-dir>` 判定；烧录 `EXO_MOTOR_ROS_ENTITIES=ON` 固件后，启动 micro-ROS Agent，确认 `/motor` 三个 topic 与 `/com/tp_mcu_status` 同时可见；发布空 `frame_id` 的 `JointTarget` 验证 `JointState.last_target_seq`、TTL stale、clamp/fault；注入非空 `frame_id` 验证被干净拒绝；再跑 200Hz enabled target soak，验证 `targets_received` 约 200Hz 增长、`targets_applied` before/mid/after 持续增长、最终 `last_target_seq` 跟随最新 seq、`/com/tp_mcu_status` 并发不掉线；最后做 921600/2000000 baud 下 200Hz target + configurable state/health telemetry 并发和 `/com` 并存稳定性。
- M2 静态资源现状：`build-motor` 约 `flash=94596B`、`ram_static=18156B`；`build-motor-opt` 候选约 `ram_static=16620B`，但 stack/linker reserve 不能无真机水位证据改默认。motor 默认静态通信预算显示 200Hz target + 20ms state + 200ms health（50Hz/5Hz）在 921600 baud 约 `36.79%`，超过 30% 预算；2Mbps 约 `16.95%`，静态通过；`tools/motor-m2-telemetry-sweep.py --pass-only` 显示低遥测候选 500ms state + 1000ms health 在 921600 baud 静态约 `29.91%` 且 only thin margin，需真机验证。
- 硬件不可见期间的 M2 离线优化入口是 `docs/plan/004-M2离线内存与通信预算收敛.md`：继续收敛 `build-motor`/`build-motor-opt` 静态资源、M2 ROSIDL metadata 拆分和 telemetry period 候选，但不得把 build、静态预算或工具 PASS 表述为 `/motor` 真机验收完成。
- 历史记录：2026-07-09 14:48 曾恢复 ST-LINK/SWD；Windows 设备管理器可见但 WSL `lsusb` 不可见时，根因是 ST-LINK 只处于 usbipd `Shared`、未 `Attached`；执行 `usbipd attach --wsl --busid 4-4` 后 `tools/diagnose-swd.sh` 变为 `SWD_STATUS=ok`，`chipid=0x410/dev-type=STM32F1xx_MD`。当前硬件状态必须以最新 `tools/diagnose-swd.sh` 或 `tools/com-status-report.sh <tag>` 的实时探测为准。
- 高频 profile 已达到当前目标形态：`10kHz MCU local loop + 200Hz PC cmd + best_effort + status_every_40 + 921600 baud`。2026-07-09 14:53 用 `PC_LAUNCH_PREFIX="taskset -c 1"`、`executor_threads=4` 上板实测通过 contract；14:58 不重刷 2 分钟 soak `soak_10khz_200hz_921600_taskset1_20260709_145814` 再次 PASS：PC target window `200.003Hz`，sampler `4.967Hz`（等效 MCU 接收 `198.68Hz`，status seq 进度 `199.857Hz`），PC gap `p99=5.364ms/max=10.845ms`，`lost=0 duplicate=0 qos_incompatibility=none`。同配置未绑核和 2Mbps 对照均能通信但 max gap 分别到 `58.332ms/68.031ms`，说明当前最大收益来自 PC 侧调度/绑核，不是单纯升波特率。CLI `ros2 topic hz` 偶有长尾，判据以 `status_sampler + PC summary + LinkHealth` 为主。
- 默认 reliable baseline 当前为 `qos_depth=2/startup_grace_s=3.0/rtt_deadline_ms=120`。overnight no-flash 当前 19 个样本为 12 PASS / 1 WARN / 6 FAIL：第 5 轮出现一次运行期长尾并 FAIL（`lost=1`、`seq_delta_max=2`、`sampler_max_gap_s=0.134`，对应 LinkHealth `seq=116 waited_ms=130.808` 超过 120ms hard deadline）；第 8 轮出现更明显的主机/链路长尾（PC publish gap `max=307.950ms`、`sampler_max_gap_s=0.358`、`lost=1 duplicate=1`）；第 9 轮频率基本正常但 `seq=124 waited_ms=155.411` 触发 `lost=1 duplicate=1`；第 12 轮 sampler 与 PC gap 也基本正常（`20.102Hz/max_gap=0.078s`、PC gap `max=78.015ms`），但 `seq=248 waited_ms=123.840` 刚超过 120ms；第 13-16 轮连续 PASS；第 17 轮 sampler 与 PC 发包仍干净，但 `seq=143..150` 连续 8 条 full-echo 超 120ms，最终 `lost=8 duplicate=3`；第 18 轮再次出现更像主机/链路暂停的长尾（sampler `max_gap=0.350s`、PC publish gap `max=300.424ms`、`LOST seq=128 waited_ms=300.621`、`lost=1 duplicate=1`）；第 19 轮无 lost/duplicate，但 sampler `max_gap=0.284s`、status seq `max_delta=4`，summary 记 WARN。结论是串口/ROS 链路不是持续断开，但 full-echo reliable 回包/匹配路径仍有偶发 burst 长尾，且主机/WSL 调度/status 接收长尾会触发 WARN/FAIL；判据应以 sampler + PC summary + LinkHealth failure events 交叉看，taskset 绑核是后续整夜对比候选。
- 2026-07-09 09:02 的 no-flash PC 调度短 sweep `pc_sched_after_overnight_20260709_0902` 跑了 baseline 与 `taskset -c 2` 各 2 轮，4/4 PASS。baseline 两轮 PC gap 为 `p99/max=58.167/75.092ms`、`53.891/60.350ms`；`taskset -c 2` 两轮为 `50.942/54.982ms`、`50.683/54.953ms`。短测支持 `taskset` 能收紧 PC 发包抖动，但不能替代整夜对比；下一组 overnight 建议用 `PC_LAUNCH_PREFIX="taskset -c 2"`。
- 随后 09:07 跑短版 taskset mini-watch `taskset_minwatch_20260709_0907`（3 samples，5 分钟间隔）得到 2 PASS / 1 FAIL：第 2 轮在 PC gap 仍很紧（`p99/max=51.208/66.957ms`）时出现 full-echo burst lost，`seq=149..155` 共 7 条超过 120ms，最终 `lost=7 duplicate=1`，sampler `max_gap=0.352s`、status seq `max_delta=7`。结论更新：`taskset` 能收紧 PC 发包尾部，但不能单独根治 reliable/full-echo 回包/匹配路径长尾；控制链路仍应优先走 latest-target + sampled/status decimation。
- 09:26 做不烧录 latest-target 复测 `latest_target_noflash_20260709_092648`：PC 侧 200Hz timer 很稳（`wire_rate_hz=199.988`、PC gap `p95/p99/max=5.252/5.529/16.126ms`），但 `sent/matched/status=0`；日志明确报 `/com/tp_cmd_heartbeat` 上 `RELIABILITY` QoS incompatibility，说明当前板上固件仍是 reliable 订阅 profile，不是 best-effort latest-target 固件。这个失败不是串口断链，而是当前固件 profile 与 PC latest-target preset 不匹配；必须等 SWD 恢复后重刷 best-effort/status_every_40 profile 再验收 200Hz latest-target。
- 09:40 用增强后的 graph/QoS 日志复跑短版 latest-target `latest_target_qos_graph_noflash_20260709_094005`，得到更直接证据：`/com/tp_cmd_heartbeat` 上 PC publisher 是 `BEST_EFFORT`，MCU subscription 是 `RELIABLE`；PC 仍能稳定 200Hz 发包（`wire_rate_hz=199.998`、PC gap `p95/p99/max=5.211/10.115/32.641ms`），但 status/sampler 为 0，contract 明确 `qos_incompatible`。结论不变：当前板上固件不是 latest-target best-effort profile，需恢复 SWD 后重刷。
- 09:27 随后用同一当前固件跑默认 reliable/20Hz no-flash 对比 `reliable20_noflash_after_latest_mismatch_20260709_092747`：基础 ROS/串口仍在线，`status_hz=20.095`、`sampler_hz=20.028`、PC gap `p95/p99/max=50.417/51.322/55.269ms`、线速约 `18.12 kbit/s`（`baud_util=1.97%`）；但 full-echo 诊断仍 FAIL（`lost=4 duplicate=1 inflight=1 seq_delta_max=4`）。这进一步支持：裸串口带宽不是主瓶颈，reliable/full-echo 回包/匹配路径存在偶发长尾；控制链路应继续围绕 latest-target + 状态降频/采样验收。
- 当前硬件卡点已解除：ST-LINK/SWD 和独立 USB-TTL 均在 WSL 可见；若后续再次变为 `bad_no_stlink`，先在 Windows/WSL 侧执行 `powershell.exe -NoProfile -Command "usbipd list"`，确认 ST-LINK busid 后执行 `powershell.exe -NoProfile -Command "usbipd attach --wsl --busid 4-4"`。若变为 `bad_unknown_target`，再查供电、复位、BOOT0、SWDIO/SWCLK/NRST 和共地。
- 当前内存基线：`tools/firmware-size-report.sh` 显示默认 ELF量级约 `flash≈82.5KB`、`ram_static≈14.5KB`；最大 RAM 符号为 `microros_task_stack=3072B`、`custom_sessions=1192B`。`tools/firmware-size-matrix.sh staircase_20260709_013954` 显示 2/5/10kHz profile 因切到 TIM2 ISR、去掉 `control_task_stack`，静态 RAM 降到 `13912B`，比 1kHz task 模式少约 600B；best_effort/status40 对静态 RAM 基本无影响。默认 reliable 栈水位曾测到 micro-ROS 栈 used≈484/free≈284 words（总 768）；是否从 768 继续压到 640 必须等高频 profile 栈水位复测后再决定。
- 当前线速基线：`MICROROS_AGENT_VERBOSITY=6` 的 no-flash 默认 20Hz reliable 短测通过 `tools/agent-wire-stats.sh` 解析，最新 same-tag wire 样本为 `15.98 kbit/s`、占 921600 baud 的 `1.73%`（早前同类样本约 `13.66 kbit/s/1.48%`）。因此默认 20Hz 的 20ms 级 RTT 不是裸串口带宽打满；高频优化仍优先围绕 latest-target、状态降频、QoS/stream/调度做实测。
- 当前 no-flash 200Hz 边界：在板上默认 reliable/status_every_1 固件不重刷的情况下，PC 可 `wire_rate_hz≈200Hz`，status seq 进度反推 MCU 收到目标约 `201Hz`，但状态回传只约 `32Hz`，full-echo LinkHealth 会大量 lost；线速约 `91.61 kbit/s`、占 921600 baud `9.94%`。随后 20Hz 回归恢复 `sampler_hz=20.004`、`p99_gap_s=0.056`。这进一步证明高频控制要用 latest-target + 状态降频/采样，不能要求 200Hz reliable 每条命令都回状态。
- no-flash 参数复测确认：`cmd_catchup_max=1` 能把 PC wire 均值拉到约 200Hz，但在当前 reliable/status_every_1/full-echo 固件上会把状态回传压到约 2.6Hz、线速占用升到约 36.65%；它只适合 best-effort/status decimation/sampled 的吞吐对比实验，不应作为 reliable 默认诊断参数。2026-07-09 no-catchup PC-only sweep `pc_sched_nocatchup_20260709_1129` 显示 200Hz latest-target 发包 catch-up 为 0，default case window `199.997Hz`、gap `p95/p99/max=5.210/8.468/19.747ms`；因此当前 latest-target 默认改为 `cmd_catchup_max=0`。
- PC scheduler sweep 已支持 `PC_SCHEDULER_CASES='label|prefix|executor_threads'` 的逐 case 线程数覆盖。短测 `pc_sched_threads_20260709_1137` 比较 default、`executor_threads=2`、`taskset -c 2 + executor_threads=2`，当前最佳为 `threads2`：PC window `199.998Hz`、gap `p95/p99/max=5.222/5.937/16.047ms`、catch-up `0/0`；`taskset+threads2` p99 也低但 max 到 `31.998ms`，暂不优先。
- 2Mbps 高频 profile 已上板对照：`EXO_UART_BAUD=2000000 + EXO_CONTROL_LOOP_HZ=10000 + best_effort + status_every_40` 可建链，PC target window `197.175Hz`，sampler `4.837Hz`（等效 `193.48Hz`），`lost=0 duplicate=0`，但 PC gap `p99=13.665ms/max=68.031ms`，没有优于 921600+taskset。结论：200Hz 控制目标先固定 921600，优先收紧 PC 调度和长尾；2Mbps 保留为未来 500/1000Hz 或线速预算更紧时的候选。
- UART read 低延迟候选已参数化：`EXO_UART_READ_POLL_YIELDS=4` 会在 1-tick sleep 前先 `taskYIELD()` 快速轮询，编译验证静态 RAM 不变、Flash +80B；是否改善 RTT/gap 长尾待 SWD 恢复后上板实测。
- LTO 独立构建试验 `build-lto-check`（10kHz/best_effort/status_every_40）可编译，但收益不适合当前默认：Flash 约从 `82360B` 降到 `82000B`（省约 360B），静态 RAM 反而从 `13896B` 增到 `13912B`；`rosidl_type_metadata/custom_sessions/microros_task_stack` 三个 SRAM 大头不变，因此 LTO 暂列为低优先级候选。
- DWT timestamp 回绕扩展已从“hi/last 直接共享”加固为双缓冲 generation 快照发布，消除高优先级读者可能读到半更新态的隐患；编译验证默认 profile 为 `flash_bytes=82440/ram_static_bytes=14496`，高频 best-effort/status_every_40 为 `82280/13896`；`tools/test-dwt-snapshot-model.sh` host-side 模型测试 PASS。
- micro-ROS 栈候选已可用 `tools/firmware-stack-sweep.sh` 静态比较：10kHz/best-effort/status_every_40 下 768/704/640 words 对应 `ram_static_bytes=13896/13640/13384`。默认仍保持 768 words；704/640 必须等 SWD 恢复后复测高频 HWM 再决定。
- 最新静态候选复测：`spin_20260709_014137` 显示 `EXO_EXECUTOR_SPIN_TIMEOUT_US=1000/500/200/100` 静态 RAM/Flash 完全相同（10kHz latest-target 下 `flash=82328B/ram_static=13912B`），是运行期延迟/CPU 占用候选；`stack_20260709_014152` 显示 micro-ROS stack 704/640 words 可分别省 256/512B；`reserve_20260709_014210` 显示 heap/MSP reserve 理论可省 768B-1024B，但风险更高，必须等 SWD 恢复后结合 HWM/HardFault 证据。
- 离线回归入口已固化为 `tools/test-offline-contracts.sh`，覆盖 control target 模型、control-loop staircase 配置、DWT snapshot、micro-ROS 配置契约、线速/线时预算、性能契约、summary/parser、staircase dry-run、README 文档合同、handoff 报告、firmware size report 和 firmware memory optimization summary；当前 PASS。ROS 工作区 `colcon test --packages-select exo_cmd com_bringup exo_msgs` 当前 PASS；最近一次 `colcon test --packages-select com_bringup` 为 5 tests 通过，新增 launch preset 静态检查锁住 `pc_latest_target` 的 high-rate 默认值。
- 可用 `tools/com-status-report.sh morning_$(date +%Y%m%d_%H%M)` 生成明早交接报告，报告会汇总当前 git 版本、最近提交、SWD 状态、串口设备、最近 no-flash 指标、M2 motor build/通信预算、telemetry period sweep、overnight contract/趋势、staircase preflight/阶梯汇总、静态内存矩阵、栈候选和未解决项；只看 blockers 时用 `tools/summarize-com-unresolved.sh morning_$(date +%Y%m%d_%H%M)`。
- 最近已推送的关键提交：`46009c7` 接入 motor micro-ROS 实体、`975b2d7` 加固 motor 输入和 snapshot、`7bba9ca` 暴露 motor 通信预算、`73a2846` 修复 `Found 0 stlink programmers` 误报可跑 staircase 的 preflight bug、`cd203a0` 让后续 overnight watcher 等待串口锁、`82bdff0` 在 handoff 报告中显示 overnight watcher contract。
- 高频 profile 栈水位已复测：`tools/measure-stack-hwm.sh firmware/f103-microros/build/f103-microros.elf` 在 10kHz/best-effort/status_every_40 固件上读到 `microros_task_stack used=286/free=482 words`（总 768）、`led_task_stack used=22/free=42`、`idle_task_stack used=24/free=72`；`control_task_stack` 在 10kHz TIM2 ISR 模式下不存在，符合预期。读完后已 `st-flash reset` 恢复运行。
- 下一步优先顺序：以 `10kHz MCU local loop + 200Hz PC latest-target + 921600 + status_every_40 + PC_LAUNCH_PREFIX="taskset -c 1"` 作为当前推荐主线，补一轮更长 soak；完整 1/2/5/10kHz staircase 可作为后续验收矩阵，不再阻塞当前主目标。内存优化可把 micro-ROS stack 704/640 words 作为后续候选，但先不要改默认。

### 已锁定的决策(不要再反复澄清,直接据此设计)
- ROS2 发行版:**Jazzy**(LTS),跑在新装的 **Ubuntu 24.04** WSL 发行版。
- MCU:**ST Nucleo-F103RB**(STM32F103,**2026-06-19 从 F401 换芯**)。工具链:**裸 CMake + arm-none-eabi-gcc**(不用 CubeIDE)。RTOS:**FreeRTOS**。
  - 换芯影响详见 `docs/01-ros2-microros-serial/04-变更-F103换芯与硬件到位.md`(elon 设计)。要点:Cortex-M3 无 FPU(flag 改 `-mcpu=cortex-m3`/无 `-mfpu`/`-mfloat-abi=soft`、固件禁浮点);HAL 换 stm32f1xx 并按 F1 模型重写 GPIO/USART/DMA;72MHz/APB1=36MHz 下 USART2@921600 误差仅 +0.16%,无需改波特率;USART2=PA2/PA3 经板载 ST-Link VCP,**一根 USB 即通**,引脚不变。
  - **⚠️ 项目级真风险:F103RB 只有 20KB SRAM**(micro-ROS 官方推荐 ≥32KB)。需激进裁剪 colcon.meta(pub/sub=1、history=1、static pool、小 MTU)+ 砍浮点;**且先做去风险 spike(T8)回答"20KB 跑不跑得下"**,有压不进的真实可能,降级预案见 04 文档 A.2。
- micro-ROS:以**真实 STM32 工程**方式导入(产物可直接烧板,非 Ubuntu 模拟)。固件虽裸 CMake,但 micro-ROS 静态库生成依赖 ROS2 工具链,已接受。
- 串口:**921600**,启用 **DMA**,**双向**。USART2=PA2/PA3(VCP);备选独立串口 USART3=PC10/PC11(原 F401 方案的 USART6 已随换芯替换)。
- micro-ROS Agent 跑在 ROS2 侧(WSL),传输层 **serial(XRCE-DDS over serial)**。
- **现实边界已变(2026-06-19):用户已把 Nucleo-F103RB 通过 USB 接到运行 WSL 的 Windows 上,Phase B 真机联调解锁。** 但 WSL 还没透传——需 **usbipd-win** 把 ST-Link VCP attach 进 WSL2 才会出现 `/dev/ttyACM0`(步骤见 04 文档 B 节)。实测当前 WSL 内尚无 `/dev/ttyACM*`。
- **两个待用户拍板的决策点(见 04 文档末)**:① F103 侧 QoS history 深度 10→1(WSL 侧保持 10、RELIABLE 不变)是否接受;② T8 若判定 20KB 跑不下,默认回来找用户拍板(降功能/换协议/换芯),Tom 不自决——确认此流程。

## 当前进度 / 卡点(更新于 2026-06-18)
1. ✅ 迁移完成:现已在 **Ubuntu 24.04.4 LTS**(用户 `lhq24`,家目录 `/home/lhq24`,项目 `/home/lhq24/robotics`)。
2. ✅ **elon 已产出架构文档 + 接口契约 + Tom/Gill 任务卡**,落盘在 `docs/01-ros2-microros-serial/`:
   - `00-架构文档.md`(系统框图、双向 topic 设计、USART2 引脚选型、目录结构、阶段划分)
   - `01-接口契约.md`(topic/类型/频率/QoS、串口 8N1/921600/DMA、agent 起法——开发测试共同基准)
   - `02-任务卡-Tom.md`(T1~T7)、`03-任务卡-Gill.md`(G1~G7)
   - (落盘由主 agent 代笔:elon 子 agent 在当前权限模式下无法弹交互授权写盘,设计内容为 elon 产出。)
3. 环境实测:ROS2、arm-none-eabi-gcc、codex CLI **均尚未安装**——这是 Tom 的 **T1** 任务卡内容。
4. 旧发行版打包中转目录(如需再取):
   - `/mnt/c/Users/Lenovo/wsl-migrate/home-config.tar.gz`(`.ssh`/`.gitconfig`/`.codex`)
   - `/mnt/c/Users/Lenovo/wsl-migrate/robotics.tar.gz`(本项目)

## 进度快照（截至 2026-06-18 下班）
1. ✅ **T1 完成**:ROS2 Jazzy / colcon / arm-none-eabi-gcc 13.2.1 / cmake 3.28.3 / ninja / make / st-flash 1.8.0 / openocd / codex 0.140.0 均装好,`tools/env-check.sh` 关键组件齐备。装机用 `tools/install-toolchain.sh`(用户交互终端跑,sudo 需密码)。
2. ✅ **T2 完成 + G2 有条件通过**:`ros2_ws/src/{exo_cmd,com_bringup}` 基础回环跑通(colcon build ✅、`/exo/mcu_status` 单调递增、QoS RELIABLE/KEEP_LAST/10 经 applied-QoS 日志铁证)。G2 三方交叉验证(Gill 静态 + 主 agent 实跑 + Codex),发现 P1-1/P1-2/P1-3 三个遗留项。详见 Gill 任务卡 G2 条目。
3. ✅ **契约升级 v1.1**:elon 增补第 7 节「链路健康监控」(安全关键),把回环监控从"值对得上即可"升级为实时控制环标准——延迟可测/超限可报/丢包显式检出/**禁止静默驱逐**/对账等式 `sent==matched+lost+inflight`。落实 P1-1/P1-2/P1-3。验收点 A1–A8(全 Phase A 可测)。见 `01-接口契约.md` 第 7 节。
4. 🔵 **Tom 已实现 v1.1 监控**:新增 `exo_cmd/link_health.py`(rclpy-free `LinkHealthTracker`)、重写 `exo_cmd_node.py`(删 min() 静默驱逐,薄适配层)、`loopback_node.py`(加故障注入 param: inject_delay_ms/drop_seqs/drop_rate/duplicate/seed)、`test/test_roundtrip_logic.py`(覆盖 A1–A8)。

## 进度更新（2026-06-19）：昨天的卡点已解决
昨天怀疑的「loopback 延迟注入 bug / one-shot timer」**是误判**——延迟注入机制本身正常。主 agent 实跑后定位到**两个互相独立的真 bug**:

1. **测试 harness 孤儿串台(主 agent 修)**:`ros2 run` 把节点拉成子进程,`kill` 只杀父包装进程,真节点变孤儿继续在 DDS 图广播;`pkill -f <节点名>` 还会匹配脚本自己导致清理自杀(exit 144)。多个孤儿 exo_cmd 各自从 0 发心跳互相串台 → 指标爆炸(8s 内 sent=6000+、duplicate 8 万),长得像逻辑 bug 实为环境污染。**昨天的 matched=0 主要是这个。** 已在 `tools/run-scenario.sh` 用 **setsid + 按进程组 `kill -- -PGID` + 跑前 `pgrep` 硬断言无孤儿**根治,复跑实测零孤儿。教训已存记忆 `ros2-verify-kill-process-group-or-crosstalk`。
2. **`loopback_node.py` 的 `drop_seqs` 声明崩溃(Tom 修)**:空列表默认值被 rclpy Jazzy 锁成 BYTE_ARRAY,传整型数组就崩、节点起不来 → A3 确定性丢包从声明之日起没真正可用过(逻辑单测照不出)。Tom 改用 `ParameterDescriptor(dynamic_typing=True)`,已 rebuild。

**rebuild 后干净复跑 5 个链路监控场景全绿**(setsid 隔离、零孤儿、零 UNMATCHED、RECONCILE BROKEN=0):
- A2 延迟 60ms:matched=69 lost=0,全程 WARN(RTT≈61ms) ✅
- A3 丢 seq[5,6,7]:lost=3,**丢的正好是 5/6/7** ✅
- A3b 丢包率 20%:matched=54 lost=15 ✅
- A5 echo×2:duplicate=69 全检出 ✅
- A2 延迟 250ms(>200 deadline):先判 LOST、迟到 echo 计 duplicate,对账 `70=0+68+2` ✅ 合理

**留的无害收尾小项**:节点 `main()` 只 catch `KeyboardInterrupt`,SIGTERM 关停打 `ExternalShutdownException` 栈(功能正常)。Phase B 用 systemd 停服务想干净退出,可顺手补 catch(exo_cmd_node / loopback_node 都是)。

## ✅ A1–A8 对抗验收完成（2026-06-19，gill + 主 agent colcon test 复核）
- **A1–A8 逻辑层全部 PASS**。gill 新增 `ros2_ws/src/exo_cmd/test/test_link_health_adversarial.py`(20 对抗用例:极端时序/同 tick、禁静默驱逐最旧+逐条告警+cap=1、2^31 边界与强制 wrap 碰撞、参数校验、500 步随机混合每步断言对账恒等),主 agent `colcon test` 复核 = 42 passed。
- ✅ **lint 门禁已全清(2026-06-19)**:Tom 分两轮清掉 82 处 flake8(import 顺序/未用 import/生成器/docstring D205/D209/D400/D401)+ 26 处 pep257 D213(摘要移到第二行,同时满足两门禁,无 D212/D213 冲突)。主 agent `colcon test --packages-select exo_cmd` 复核:**44 tests, 0 failures**(flake8 + pep257 + 全部逻辑/对抗测试全绿)。

### gill 的两个语义发现（非阻断，但已登记为 follow-up）
1. ~~**[中,对 F103 相关] `settled_window` 会把超窗的真实 RELIABLE 重传误报成 UNMATCHED**~~ → **✅ 已解决(2026-06-21,契约 bump v1.6)**。`on_echo` 加 `_ever_sent`(wrap-safe)区分:曾发出但超窗 → 新 `stale_duplicate`(独立计数 `stale_duplicate_count` + INFO,不再误报 UNMATCHED);从未发出 → 仍 UNMATCHED。**gill 复审又揪出一个 High 漏洞并已修**:负/越界 `[0,2^31)` 外的 Int32 echo 此前会被 wrap 取模 alias 成 stale 而**漏报**真 UNMATCHED → `on_echo` 入口加域校验先判 UNMATCHED(漏报比误报危险)。补 6 组对抗用例(含越界/lost+stale/混合/满wrap xfail);`colcon test exo_cmd` = **56 tests 0 fail 1 xfail**(d==0 满 wrap 误报边界,6.8 年远期,标 xfail)。**契约 §7.5② 边界策略(板子重放带内旧值判 benign)留 owner 可推翻**。
2. **[低/远期] wrap 碰撞 echo 别名**:理论存在但实际不可达(deadline 0.2s vs wrap 周期 6.8 年,旧条目早 ~10^9 倍时间前就判 LOST),现有碰撞处理是正确的 belt-and-suspenders。exo_msgs 扩宽 seq 后自然消解,不处理。
- ~~gill 还提示:`LinkHealthTracker` **非线程安全**~~ → **✅ 已加锁(2026-06-21)**。用 `threading.RLock`(可重入,counters→inflight 安全)串行化所有公共入口(on_send/on_echo/sweep_deadlines/counters/reconciles/inflight);新增 2 个并发对抗测试(8×500 on_send 零丢增量、6×400 send+echo + 并行 sweep 恒等不破)。现可安全切多线程 executor(节点仍用单线程,切换是独立决策)。

## Phase B 进度（2026-06-19，攻坚 WSL↔F103 真机 pub/sub，计划见 `05-PhaseB攻坚计划.md`）

### ✅ 已完成
1. **T3 — micro_ros_agent 装好**：Jazzy 无 apt 包，源码 build（`~/uros_ws`，micro_ros_setup jazzy 分支 → create_agent_ws → build_agent）。`ros2 run micro_ros_agent micro_ros_agent` 可运行（无参打 Usage）。装机依赖需用户 sudo：`bison clang-tidy curl flex libcurl4-openssl-dev libncurses-dev python3-vcstool python3-yaml usbutils`。脚本 `tools/run-bridge.sh`（默认 `/dev/ttyUSB0 @921600 -v6`）。
2. **T4 — F103 裸机骨架，TX 侧全绿**：寄存器级（CMSIS only，无 HAL）。第三方 vendor 在 `firmware/f103-microros/ThirdParty/`（CMSIS_5 5.9.0 + cmsis-device-f1 v4.3.5 + FreeRTOS-Kernel V11.1.0）。**build OK（FLASH 3.9% / RAM ~30%，给 micro-ROS 留 ~14KB）；时钟树 72MHz+HSE bypass、FreeRTOS、LED 都正常；USART TX @921600 完美**。
   - 修了 Tom 两个 bug：① BRR 漏 16 倍过采样（USART2 0x271→0x27；后改 USART1=0x4E）；② 启动横幅/复位瞬态。
3. **契约升 v1.3 + 通信口架构变更（用户拍板）**：因 ST-Link VCP+usbip+老固件导致 RX 收不到，改为**独立 USB-TTL 上的 USART1（PA9=TX/PA10=RX，DMA1 Ch4/5，BRR=0x4E）；ST-Link 仅 SWD 烧录**。接线：USB-TTL RXD←PA9(D8)、TXD→PA10(D2)、GND 共地，不接 VCC。两个设备都 usbipd attach（ST-Link=`/dev/ttyACM0` 仅烧录、USB-TTL=`/dev/ttyUSB0` 通信）。

### ✅ 已解决：USART RX 故障 = 两个 DMA RX 固件 bug（2026-06-20 真机查清并修复）
**现象（历史）**：MCU TX 完美，但 host→MCU 方向，发任何字节（0x55/0xFF/'A'）→ MCU 都读成 **0x00 + 字节爆炸**（发 4 收 ~226），9600/115200/921600 各速率都一样坏。
**真实根因（已确诊，不是硬件/PA10/信号完整性问题）**：两个独立的 DMA RX 固件 bug。
1. **主因（CPAR 被静默丢成 0）**：`DMA_Init()` 里 `RCC->AHBENR |= RCC_AHBENR_DMA1EN;` 开 DMA 时钟后**紧跟着写 DMA 寄存器**，开时钟后头一两个外设写被 STM32 静默丢弃 → DMA1 Ch5（RX）的 `CPAR`（外设地址）被丢成 `0` → DMA 从地址 0 狂读而非读 `USART1->DR` → RXNE 永不清 → 第一个字节一到，DMA 自由奔跑无限搬 `0x00`。**这正是「TX(Ch4) 完美、RX(Ch5) 坏」那个诡异不对称的真正原因**：TX 的 CPAR 写得晚、时钟已稳，所以一直正常。**修复**：开时钟后加 `(void)RCC->AHBENR;` 回读屏障。
2. **次因（CNDTR 绕回死循环）**：`rx_dma_collect()` 的 `cur = SIZE - CNDTR` 在 circular 绕回瞬间能取到 256，而索引被 `&255` 掩在 `[0,255]` 永不相等 → while 死循环。**修复**：cur 也加掩码。
**已验证**：修复后 **9600 / 115200 / 921600** 全部逐字节精确双向 echo（含 `0x00..0xFF` 全值域、256 字节连续）。
**纠正之前的错误归因（留痕）**：此前曾把故障怀疑成「PA10 引脚硅损伤 / 高波特率信号完整性不足」，并据此把契约降到 115200（v1.4）。**该归因是错的**（基于一份错误的现场诊断），与波特率、PA10、信号完整性均无关。真因是上述 DMA bug，已修复。波特率**定回 921600**（原始设计目标，实测稳定），契约已 bump 到 **v1.5**。

### ✅ T5 双向建链全线打通（2026-06-21，commit fdfbfba）
排查"建链卡死"用硬件调试器逐层定位到 **4 个 colcon.meta 根因**（需重建 libmicroros）：
① `UCLIENT_MAX_*_BEST_EFFORT_STREAMS 0→1`（=0 使 best-effort 流零长 → rmw_init 踩穿相邻 reliable buffer → 写 0x80 → HardFault，被 CMSIS 弱默认 handler 伪装成"静默 hang"，这也是旧"agent 拒所有帧"悬案的真相）；② `RMW_UXRCE_CREATION_MODE refs→bin`（vanilla agent 开箱即用，免 XML profile）；③ `RMW_UXRCE_STREAM_HISTORY 1→2`（reliable 输出流缓冲 128B 塞不下 create_topic bin 消息 → publisher 创建失败；HISTORY=2 → 256B 经 XRCE 分片，串口 MTU 仍 128 不破契约 v1.5）；④ `ROSIDL_TYPESUPPORT_SINGLE_TYPESUPPORT ON→OFF`。固件侧永久保留：有界 `_sbrk`、`HardFault_Handler` 发 `!!HARDFAULT!!` 标记取代弱默认死循环、micro-ROS 栈 6KB。

### ✅ T8 / M4 go/no-go = **GO**（2026-06-21 主 agent 真机量化，fresh T5 build）
**结论：20KB 跑得下，且裕度极大。** 全程证据（板载 `/exo_mcu`，vanilla agent over `/dev/ttyUSB0@921600`）：
- **静态内存**：Flash **77520 B / 128KB = 59.14%**；RAM(.data+.bss) **14832 B / 20KB = 72.42%**（较早期"97.3%"大降，得益于 RAM 优化 commit 336763a：栈 10→6KB、FreeRTOS heap 2KB→512B）。
- **动态分配 = 零**：gdb dump RAM 实测 `_sbrk` 的 `heap_end=0`、`__malloc_sbrk_start=0`、`ucHeap` 全零 → **newlib heap 与 FreeRTOS heap 全程从未被调用**，micro-ROS 完全跑在静态内存池上（colcon.meta 静态配置完美生效）。⇒ 14832 B 已涵盖一切，余 **~5.6KB 纯余量**（仅 MSP/ISR 栈用，heap 永不增长，无堆溢出风险）。
- **栈水位**（FreeRTOS 0xA5 fill 扫描，soak 后活态）：uros 任务 **峰值 285 words(1140B) / 1536w，余 1251w**；led 21w、idle 20w。栈从不是瓶颈。
- **建链**：session established=1，participant/2×topic/publisher/datawriter/subscriber/datareader 各建一次且稳定不重连。
- **双向闭环（权威 LinkHealthTracker 对账，G-T7 bidi 等价）**：真 `exo_cmd_node`(v1.1 LinkHealthTracker) 驱 10Hz、板子 echo。**40s：sent=388 matched=387 lost=0 duplicate=0 零 UNMATCHED**；**10 分钟 endurance soak：sent=6009 matched=6008 lost=0 duplicate=0、零 UNMATCHED、对账恒等 `sent==matched+lost+inflight` 全程成立、6008 条独立 echo ground-truth**。inflight=1 是 10Hz 稳态最后一条在途（非停滞，matched 与 sent 锁步）。安全关键"零丢包预算 EXO_LOSS_BUDGET=0"满足。
- **无重连/无 fault**：10 分钟内 agent 真实 `create_client=1`、agent 日志零 session loss/disconnect/timeout、零 HardFault；早期固定值 3min soak 亦 1816/1816 全对。
- 量化工具（可复现）：`tools/run-bridge.sh`；gdb 读 RAM = st-util :4242 + gdb-multiarch dump 0x20000000..0x5000，扫 0xA5 栈 + 读 heap_end（`heap_end.2`@0x20000688、`end`@0x200033f0）；bidi 对账 = `$CLAUDE_JOB_DIR/tmp/bidi_recon.sh <秒数>`（域 0、起 agent+exo_cmd_node、读 tracker 对账）。

> **✅ 两个 hw_acceptance.sh 待修项已修复（2026-06-21，commit b61434d）**：① 默认 `ROS_DOMAIN_ID` 42→**0**（固件 participant 在域 0，强制 42 致 UNI 看不到 /exo_mcu；隔离改靠 `ROS_LOCALHOST_ONLY`）。② re-session 检查 `grep -ciE "create_client|client_key"` → **只数 `create_client`**（每条 `-v6` debug 行带 `client_key:`，旧式计千行误判正常运行为"板子反复复位"；实测修后 =1）。同 commit 另修 4 处真机鲁棒性(DEV 默认 ttyACM0→ttyUSB0、QoS 桥接端点观测不到时降级为 NOTE 续跑、warmup 基线扣除握手期丢包、drain 放宽到 inflight>3 才 FAIL)。

## 进度更新（2026-06-22）：无硬件日 —— ⑤ 多线程 executor + ③ nonce + ④ exo_msgs M-A 落地
公司环境无 STM32,做 WSL 侧零硬件依赖工作。按 elon(设计)/tom(实现)/gill(对抗验收)+ 主 agent(build/实跑/提交) 分工跑通,全部已提交到 main。

- ✅ **③ hw_acceptance.sh 两 bug** 实为 2026-06-21 `b61434d` 已修(文档此前漏更);本次新增 `start_value` 运行 nonce(`-1`=随机 31bit)+ 脚本因果性交叉校验,落实脚本 L431 RECOMMEND。commit `13e38b8`。
- ✅ **⑤ 多线程 executor**:exo_cmd_node 切 `MultiThreadedExecutor`,rx 订阅独立 `MutuallyExclusiveCallbackGroup`(echo 不再排在心跳/sweep/summary 后),`executor_threads` 参数;main 捕获 `ExternalShutdownException` 顺手解决 SIGTERM 打栈遗留项。commit `13e38b8`。实跑:nonce 生效、链路正常、SIGTERM 干净退出。
- ✅ **④ exo_msgs M-A(WSL 侧,gill 判 GO、无阻断)**:elon 设计草案(`06`)+ Q1–Q9 用户拍板(seq=uint32 / stamp=uint64 单调纳秒 / payload=最小 int32 / crc 预留默认关 / 诊断 topic 纳入 / 硬切+tag 退路 / RTT 权威不测单程 / M-A·M-B 解耦)→ M-A 任务卡(`07` Tom / `08` Gill)→ tom 实现 → gill 对抗验收(+Codex 跨厂商复审)= **GO**。commits `fe2326f` / `d8f1d1c` / `97a3167`。
  - **交付**:新 `exo_msgs` 包(`ExoHeader` uint32 seq+uint64 stamp_mono_ns+uint32 crc / `ExoCmd` / `ExoStatus` / `LinkHealth`);`link_health.py` `SEQ_MODULUS` 2^31→**2^32** 全域重标定 + 有界 RTT 滚动窗口(rtt_last / p95 nearest-rank / max);节点迁移 ExoCmd/ExoStatus(`_on_status` 喂 `header.seq`、与 payload 解耦)+ `/exo/link_health` publisher;`crc.py` 应用级 CRC-32(默认关,定位=抓打包 bug,**非**防链路误码);loopback 迁移;契约 **v1.7**(§D7 8 条 + 新增 §7.9 CRC + A9–A12)。
  - **验证**:colcon test **81 tests 0 fail 0 skip**(含 flake8/pep257/copyright);loopback 实跑 A2 延迟 WARN + A3 丢包 lost=3 精确 + 对账恒等 + `/exo/link_health` 字段全对(reconciles=true、p95<max)。gill follow-up M1(`_ever_sent` 满 wrap d==0)/L1(start_seq 上界)/L2(注释)已修(`97a3167`,原 1 xfail 翻正常 pass)。
- 🏷️ **git tag `int32-baseline`** 已打:M-B 切固件前真机可 checkout 回已验证的 Int32 链路。

## 进度更新（2026-06-22 下午）：真身 agent QA 复核 —— 6/22 产出原系"扮演版",已独立复核 + 修缺陷
**根因**:6/22 上午那批工作(⑤多线程 executor / ③ nonce / ④ exo_msgs M-A 全程 / M-B 卡起草)用户误在**家目录 `/home/lhq24/` 开 claude**,项目级 elon/tom/gill 不在作用域 → 系统静默回退成 `general-purpose` 通用 agent 靠"读角色文件扮演"产出(查会话 transcript:6 次子 agent 调用全是 general-purpose)。教训已存记忆 [`open-claude-in-project-dir-for-named-agents`]。**6/19–6/21 的固件/前期工作是真身或主 agent 直接做的,不在此范围。**

真身 elon + gill 重新独立复核 6/22 产出,结论 **已落地代码/契约本质健康(无需重做)**,但抓出并修掉若干"无记忆连续性/实跑绿了就放松"型缺陷:
- ✅ **代码层(真身 tom 修,主 agent build+实跑验证)**:
  - **High-1**:`/exo/link_health` 原分三次加锁拼快照 → 多线程 executor 下字段可能来自不同时刻(撕裂、可掩盖丢包)。加 `LinkHealthTracker.snapshot()` 单锁内一次取全;`_on_link_health`/`_on_summary` 改调它。**实跑两帧 reconciles 自洽** + 单测守恒。
  - **Med-1**:loopback 延迟 echo 用 `cancel()` 没 `destroy_timer()` → timer 泄漏(inject_delay>0 每秒漏 ~10)。改 `destroy_timer`。**实跑:300 次调度 timer 稳定收敛 ~7、结束归 0、`destroy_timer` 回调内销毁自身无异常**。
  - **Med-2**:补 CRC 端到端集成测试(两端开 crc、走 loopback 重签真实路径,非节点自造)。
  - **Low**:补两测试文件 copyright 头、删 package.xml 残留 std_msgs 依赖。
  - 复核后 `colcon test exo_cmd` = **84 tests 0 fail 0 skip**(81→84)。
- ✅ **文档层(真身 elon 修)**:M-B 卡埋的雷在开工前拦下——
  - **🔴 B1**:M-B 卡漏评"exo_msgs 比 Int32 大的 bin create 描述消息能否塞进 `STREAM_HISTORY=2`(256B)"——正是 T5 ③ 同源坑。`09` 新增流缓冲前置闸门(任务 5.0,与重建强绑)。
  - **H2**:creation mode 两出路排序((a) 喂 agent 首选 / (b) 改 xml 为下策,回退 T5 决策成本 + 确认门"被迫选 (b) 须回报拍板")。**H3**:DWT CYCCNT 32 位回绕(~59.65s)若只靠调用点比较,链路断流>59.65s 会漏回绕致 stamp 倒退 → 改独立高频源维护 64 位累加;`10` V5 加">60s 静默后单调"对抗验收。
  - 契约 `01` 五处标注实现状态(固件侧 wire 行为待 M-B)+ CRC 盲区声明(不覆盖类型/方向)。
  - 新增 **`11-exo_msgs-MB放行前复核.md`**(M-B 开工前必读 checklist)。
- ✅ **顺手清掉扮演版遗留孤儿**:发现 4 个 ROS 节点从 6/22 13:42 在默认域跑了 ~4 小时(扮演版"实跑验证"没按进程组 kill,违反 [`ros2-verify-kill-process-group-or-crosstalk`])——意味着**扮演版 Gill 当初的"实跑通过"是在串台污染环境里测的**,这也强化了重新验证的必要性。已清理,并用隔离域(DOMAIN_ID=77)重跑 smoke 拿干净证据。

## 进度更新（2026-06-24）：打 f103-baseline tag + 推远程；F407 换芯评估完成但 PENDING 等板子
- 🏷️ **新增 tag `f103-baseline`**(已推 GitHub):F103 已验证基线(T5 建链 + T8/M4 GO + M-A 绿 + 6/22 QA 修缺陷 84 tests),换芯/M-B 前退路。连同 `int32-baseline` 共两道退路。3 个 QA 提交已 push origin/main。
- 🅿️ **F407 试换芯:设计完成、整体 PENDING(等硬件)**。真身 elon 出 `12-变更-F407换芯设计.md`:策略=不分支/不 worktree,在 main 开兄弟目录 `firmware/f407-microros/`(芯片码已 100% 隔离,共享层不分叉);两个硬风险闸门=**A** libmicroros 必须 M4F/hard-float 从头重建、**B** F4 DMA2 stream 模型重写 USART RX transport(不能照搬 F1);RAM 从 20KB→192KB 是质变、无悬念。**用户手头暂无 F407 板,换芯搁置**,确认门 Q1(板型)/Q2(主频两步走)/Q4(M-B 直接在 F407 做 + plan B)未拍板,13/14 卡不起草。**期间继续在 F103 上推进。**

### 无硬件日推进(2026-06-24,出差无板子):WSL 硬化 + M-B 固件编译期 bring-up
真身 elon/tom/gill 分工 + 主 agent build/编译/量化,把不依赖板子的活先做掉。三块均已提交:
- **🥈 WSL 硬化(commit `40e1b12`,92 tests 绿)**:Med-3 cap 淘汰 `min()` O(N)→heapq O(log N)+ lazy deletion(保 §7.4 不静默驱逐、对账恒等;gill 对抗测试全过 GO);Low-3 crc_mismatch 纳入 `/exo/link_health`(计数进 tracker snapshot 同一把锁、不入对账等式、节点改 read-through property;契约 bump **v1.8** + A13 + A8 排序修)。
- **🥇 M-B 固件编译期 bring-up(commit `455c2a7`)**:microros_app 切 exo_msgs 收发、dwt_time H3 64位回绕、exo_crc `<IQi`;**重建 libmicroros 含 exo_msgs**(type support 编进、rmw config.h 确认裁剪生效)、固件编译链接通过。**关键无板验证**:① host CRC 对拍 5 向量与 WSL `<IQi>+zlib` **逐位全等**(板子日最易翻车的字节序提前消除);② 静态 size FLASH 62.77% / RAM **85.82%**(+2.7KB,比 §D6 估大——introspection 字段描述符进 .data,MSP/ISR 栈余量从 ~5.6KB 收窄到 ~2.9KB,**M4 裁 LinkHealth 可回收**)。
- **gill 价值兑现**:静态抓到 dwt `dwt_now_ns()` **回绕滞后窗口 stamp 倒退 High**(违反 V5,Codex 独立印证),板子日前修掉；后续已把 hi/last 共享状态加固为双缓冲 generation 快照，替代旧的中断优先级外部约束。
- 🅿️ **F407 换芯**:设计 `12` 完成、PENDING 等板子(同 2026-06-24,见上)。

### ⏭️ 下次续点（M-A 已绿、tag 退路就位 → ④ M-B 等硬件 / T7 真机回环）
1. **④ exo_msgs M-B(编译期 bring-up 已于 2026-06-24 无板完成——见上节;板子日只剩运行期闸门:任务 5.0 流缓冲 B1、任务 5 bin agent 桥接、建链、A1-A8、V5 长跑单调、CRC 端到端、MSP/ISR 栈实测)→ 任务卡 `09`(Tom 固件实现)/ `10`(Gill 真机验收)**:把 `exo_msgs` 喂进 micro-ROS firmware 构建(注入点 `~/uros_ws/firmware/mcu_ws/uros/exo_msgs/`)、**重建 libmicroros**(T5 同类操作,复用 toolchain.cmake);`microros_app.c` 收 `ExoCmd` 解包 / 回填 `ExoStatus`(seq 原样 / MCU **DWT CYCCNT@72MHz** stamp / payload 原样 / crc 重算);CRC 端到端字节序两端一致(显式 `<IQi` 不依赖 struct padding,最易出 bug,默认关不阻断);真机 A1–A8 在 exo_msgs 载体上复现 + RAM/ROM 量化。**★ 开工前必读 `11-exo_msgs-MB放行前复核.md`(B1/H1/H2/H3 四条硬约束 + 两处确认门)。第一闸门(任务 5.0,B1):重建后先测 exo_msgs bin create 描述消息能否塞进 `STREAM_HISTORY=2`(256B,T5 ③ 同源坑),失败按 T5 ③ 调大 + 复核 MTU128/RAM;再(任务 5)用最小 pub spike 验证 vanilla agent 桥接自定义类型,失败优先喂 agent 侧、改 xml 为下策(须回报拍板)。** 前置:真板接上 + `usbipd attach` 两设备 + 可能 sudo 补 rosdep。详见 `09`/`10`/`11` 卡 + 草案 `06` §2.2/§3。
2. **T7 真机 A1–A8 复现（G-T7,Int32 链路）**:把 `exo_cmd_node` 对接真板,跑 `ros2_ws/scripts/hw_acceptance.sh`（session/uni/bidi/endurance）+ `t8_gonogo_evidence.sh`。判据=tracker 对账 `sent==matched+lost+inflight` + datawriter/datareader 都建出。注:可用 `int32-baseline` tag 在 Int32 链路上做,与 M-B(exo_msgs 链路)互不阻塞。
3. **可选/远期**:stale_duplicate 端到端真机注入(负值 echo)验证;§7.5② 边界策略 owner 拍板;custom transport 内部 DMA 化(接口不变)。
4. ~~**⑤ 多线程 executor**~~、~~**④ exo_msgs M-A(WSL 侧)**~~、~~**落实 gill 发现 #1**~~ → 均已完成(见上 2026-06-22 / 2026-06-21 进度)。

> **✅ 固件临时调试代码已清**：T4 的 1Hz DBG 探针 + `uart_put_u32`/`rx_last_byte`/`rx_total` 已随 T5 删除（main.c），USART1 现为 micro-ROS XRCE 专用、禁任何明文输出。
> **⚠️ 烧录命令**：WSL+usbip 下烧 F103 **必须** `st-flash --connect-under-reset write build/f103-microros.bin 0x08000000`（不加 --connect-under-reset 会 "Failed to enter SWD mode"）。build：`cd firmware/f103-microros && cmake --build build`（首次 `cmake -S . -B build -DCMAKE_TOOLCHAIN_FILE=$(pwd)/toolchain-arm-m3.cmake -DCMAKE_BUILD_TYPE=MinSizeRel`）。
> **⚠️ 读 RAM 水位**：st-util 与 st-flash 共用 ST-Link，二选一；st-util 默认 halt 不 reset（RAM 保留），读完 `st-flash reset` 恢复运行。杀 st-util 用 `kill <pid>`，**勿 `pkill -f`**（自匹配致 exit 144）。

## 之后(顺序，因 F103 + 硬件到位而调整)
详见 `docs/01-ros2-microros-serial/04-变更-F103换芯与硬件到位.md` C 节。要点:
- **去风险优先**:T-meta(F103 colcon.meta 裁剪) + **T8(20KB 可行性 spike,先回答跑不跑得下)** 尽早做,可与 Phase A 收尾并行。
- T8=go → T4(F103 固件骨架,flag/HAL/时钟改 F103)→ T5(USART+DMA transport + 双向)→ T7 真机回环(含 A1–A8 真机复现 G-T7)。
- T8=no-go → 触发降级预案,回来找用户拍板。
- 「Phase A 全闭环才进 Phase B」的串行约束已可打破(硬件到位)。

## 重要环境约束(踩过的坑)
- **sudo 装包必须用户在交互终端手动跑**:agent 会话无 TTY 输不了 sudo 密码,所有 `apt` 安装都由用户执行脚本完成。
- **子 agent 的 Bash 被沙箱大幅收紧**:实测连含 `source` 的命令都被拦(即便配置已 allow);但 **主 agent 的 Bash 可放行 `colcon`/`ros2`/`source`** —— 所以"build/launch/实跑验证"这类执行交给主 agent 做,子 agent(Tom)专注写代码落盘。这是本项目稳定的分工模式。
- env-check.sh / 涉及 ROS 的脚本 source 前需 `set +u`(ROS setup.bash 引用未初始化变量,与 set -u 冲突)。
- `ros2 topic info -v` 的 History(Depth) 恒显 UNKNOWN(rmw 发现不传 depth),非 QoS 缺陷;以代码里的 QoSProfile 为准。
- 同一 WSL 上跑多份 launch 会在默认 DDS 图里串台(topic 同名互联),验证时确保只有一份实例。

### 24.04 解包命令备忘
```bash
# 在 Ubuntu-24.04 终端,家目录 /home/lhq24
cd ~
tar -xzpf /mnt/c/Users/Lenovo/wsl-migrate/home-config.tar.gz   # 还原 .ssh/.gitconfig/.codex
tar -xzpf /mnt/c/Users/Lenovo/wsl-migrate/robotics.tar.gz      # 解出 ~/robotics
# 修 SSH 权限(跨用户搬运后必须确认)
chmod 700 ~/.ssh && chmod 600 ~/.ssh/id_rsa
ssh -T git@github.com   # 验证密钥(按实际 host)
```
